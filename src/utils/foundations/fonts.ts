import { Bebas_Neue, Sen } from "next/font/google";

export const sen = Sen({ weight: ["400", "700"], subsets: ["latin"] });
export const bebasNeue = Bebas_Neue({ weight: ["400"], subsets: ["latin"] });
